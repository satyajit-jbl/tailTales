

const SectionTitle = ({heading, subHeading}) => {
    return (
        <div>
            <div>{subHeading}</div>
            <div>{heading}</div>
        </div>
    );
};

export default SectionTitle;